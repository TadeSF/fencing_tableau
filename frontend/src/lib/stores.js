import { writable } from "svelte/store";

export const pageTitle = writable("FenceWithFriends");
export const tournamentData = writable({});
export const userData = writable({});
export const standingsData = writable({});